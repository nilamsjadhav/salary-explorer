TODO

Feature 1 : Data explorer

* Create a new endpoint in backend which will return employees data to frontend (for now can be hardcoded) -> done but data of all countries in all currencies
* Consume data in frontend and display in tabular format
* implement search and filter

Create Dashboard and show cards for 
* Create script which will generate data in file 
* Save data in DB
* Store data in redux so can easily used
* Add pagination

Feature 2: Dashboard

